part of 'automation_editor_viewport.dart';

extension _AutomationEditorViewportStateOnmarkeroverlayscroll
    on AutomationEditorViewportState {
  void _onMarkerOverlayScroll() {
    if (mounted) setState(() {});
  }
}
