/// Device strip layout constants.
class DeviceStripMetrics {
  const DeviceStripMetrics._();

  /// Prior strip content height was ~72dp; sampler layout uses 2.5×.
  static const double height = 180;
}
