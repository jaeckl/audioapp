/// Rotary knob diameters — FL Studio / Ableton Note style big touch targets.
class DeviceKnobSizes {
  const DeviceKnobSizes._();

  /// Collapsed strip peek or tight layouts.
  static const double compact = 44;

  /// Device strip expanded (one tab visible).
  static const double strip = 56;

  /// Fullscreen device editor.
  static const double editor = 76;

  /// Mixer channel faders as knobs.
  static const double mixer = 60;
}
