/// How the fly-in library is scoped.
enum LibraryBrowseMode {
  /// Project resources — audio / MIDI / automation / curves / wavetables.
  resources,

  /// Device browser — instruments, audio FX, note FX (+ their presets).
  devices,
}
