/// Interaction modes for the MIDI take comp editor bottom dock.
enum MidiCompTool {
  /// Drag markers and tap take lanes to assign comp regions.
  comp,

  /// Split at playhead, nudge, delete markers; CUT/RING boundaries.
  markers,
}
